{
  "copy": {
    "excludeSourceFiles": [
      "internal/model/authz/button.go"
    ],
    "middleware": [
      {
        "sourceFile": "middleware/authz.go",
        "scope": "auth",
        "handler": "Authz"
      }
    ],
    "postNotes": [
      "Copy mode registers middleware.Authz() without arguments.",
      "Copied authz reads consts.CTX_TENANT_ID from earlier middleware by default; call middleware.SetAuthzTenantResolver(...) before serving requests when the project uses another tenant source.",
      "Use trusted tenant data such as session state, JWT claims, subdomain, or gateway-injected headers; do not trust arbitrary client headers as identity."
    ]
  }
}
