{
  "copy": {
    "excludeSourceFiles": [],
    "includeSourceFiles": [
      "internal/service/mfa/totp_login.go"
    ],
    "middleware": [
      {
        "sourceFile": "middleware/mfa_verification.go",
        "scope": "auth",
        "handler": "MFAVerificationRateLimit"
      }
    ],
    "requiredAssembly": [
      {
        "import": "github.com/hydroan/gst/authn",
        "function": "SetLoginSecondFactorVerifier",
        "reason": "enrolled accounts can log in without a second factor until the gate is installed"
      }
    ],
    "postNotes": [
      "Administrative MFA routes deny everything until the project installs its authorizer with servicemfa.SetAccountAdministrator(...) from project-owned code outside service/mfa."
    ]
  }
}
