// Package gst sits where its output file would be gst.ts, the name of the
// framework prelude.
package gst

// Item is a fixture type referenced from another package.
type Item struct {
	Code string `json:"code"`
}
